// import React, { useState } from 'react';

// function ColorPicker({ onColorChange }) {
//     const [color, setColor] = useState('#ffffff'); // Initial color state

//     // Function to handle color change
//     const handleColorChange = (e) => {
//         onColorChange(e.target.value);
//     };

//     return (
//         <input
//             type="color"
//             value={color} // Set the value of the color picker to the current color state
//             onChange={handleColorChange} // Call handleChange function when the color changes
//         />
//     );
// }

// export default ColorPicker;
